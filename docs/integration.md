# Integration/evaluation handoff

## Checkpoint status

This is an executable I/O, serialization and development-metrics boundary, **not a finished financial agent**. It makes no model calls, does not read `.env`, and has not generated public-sample or evaluation affordability predictions. Synthetic decision doubles live only in tests. No sample accuracy/generalization, final usage bill, package reproduction or CI-green result is asserted.

The canonical engineering contract is v1.1/I011: core alone owns Money, facts, financial records, recurrence, capacity and replay; evidence owns selection/decoding/private schema, the FactBatch adapter, cache/client and UsageEvents; planning owns generation/eligibility/replay adaptation/ranking; integration owns all CSV/index/envelope and output/evaluation composition. No sibling filesystem is a runtime dependency.

## Runtime/setup handoff (inbox013)

Target **Python >=3.10**, recommended **3.13**. Evidence commit `b7be3b582db2db5de7e78aa153649c34c84952d1` reports Python3.13 with Pydantic2.13.5, HTTPX0.28.1 and Pillow12.3.0; `requirements.txt` pins those direct versions within its required ranges (`pydantic>=2.11,<3`, `httpx>=0.28,<0.29`, `Pillow>=11,<13`). Inbox013 supplied dependency facts only; inbox014 subsequently authorized five exact evidence-only imports listed below. Neither handoff is a transitive lock, paid-call authority or permission for a later pipeline head. CI now targets3.10/3.13 and sets `PYTHONPATH=code`. Follow the isolated setup/test commands in README; no `.env` loading is added. Evidence's cache uses POSIX/fcntl: Linux/macOS is the supported runtime surface; native Windows cache support is not implemented or verified.

After the guarded imports, a **new worktree-local `.venv` on Python3.13.15** installed the exact pins from the public package index with config/keyring disabled and a credential-free process environment; `pip check` passed. After inbox015's complete corrected-core/planning imports, the corpus independently passes **297 offline tests** (61 core +49 prior integration +79 evidence +95 planning +13 synthetic packaging), including actual Dataset/source-locator/FactBatch/replay seams with mocked HTTP and real planning/coverage tests. This is not live extraction, supplied-option/complete-plan certification or the all25 decision baseline. Python3.10 and remote CI remain unverified; earlier3.9.6 results are historical foundation evidence only. The environment is isolated here, ignored, and not a sibling-runtime or archive dependency.

## Current executable interfaces

- `buy_wait.data.Dataset.load(dataset_root)` reads **only** the six supporting CSVs listed in `CONTEXT_FILES`. `load_requests(dataset_root)` separately reads the eight-field production request file. Both return/source-pin immutable source-string rows, not financially admitted Money. Fixed-name allowlists, exact headers, record widths, duplicate IDs, same-user event links, lifecycle cycles and dated/directed duplicate FX keys are checked. Symlink CSVs are rejected before reading. Numeric admission belongs to core, not a competing converter in the loader.
- `Dataset.context_for(request)` returns an immutable **raw input envelope** with `request`, `profile`, `events`, `options`, `fx_rates`, `message_candidates`, `image_candidates`, `source_locations`, `source_hashes`. Requests are projected using exactly the eight allowed input fields. Missing event amounts stay `""`, never zero. User-scoped candidates include unlinked and event-only sources. These are candidates, not admitted evidence: extraction still owns optional-ID matching, timestamps/as-of filtering and image bytes. Images need not exist for raw metadata inspection to pass.
- **Authoritative typed planning adapter:** `Dataset.planning_context_for(request) -> buy_wait.data.PlanningContext`. Its frozen `RequestInput` has exactly the eight input fields: IDs/type/text as strings, dates as `date`, partial permission as `bool`, amount as the **real core Money**. `PlanningPreferences` exposes `.methods`, `.max_installment_months` (blank → `None`), `.protected_categories`, `.reducible_categories`, `.stoppable_categories`, `.financial_priorities`; method/category collections are immutable whole tokens, never substring matching. `SuppliedOption` preserves the nine provider columns, parsing dates/counts/optional interval and the three monetary fields through core. Late/unaccepted offers are retained for planning to reject, not silently dropped. The envelope provides immutable `.options_by_id`, `.request`, `.preferences`, and `.context_hash`; options are stored once in stable ID order, and the core's canonical hasher hashes typed request/preferences/options. No Plan/PlanValidation/result clone is introduced.
- **Actual narrow core adapter:** `Dataset.financial_input_for(request, evidence_sources=()) -> buy_wait.contracts.FinancialInput`. It constructs the imported `FinancialProfile`, `EventRecord`, `FxRate` and CSV `SourceRef` records; delegates cent-exact/optional Money parsing to `buy_wait.core.money`; leaves unknown amounts/dates `None`; and does not perform FX conversion, cash classification or a forecast. The core view excludes offers, deadlines, payment methods/caps, partial permission and request text. Its admitted profile carries only financial balances and category protection/adjustability. Extraction's host-built message/image SourceRefs can be supplied after selection; no guessed `known_at`, actor, hold, settlement or image cash is synthesized. CSV source IDs are `csv:<filename>:<business-id>`; FX business IDs are `<date>:<from>:<to>`. Paths are relative to `dataset/`, record ordinals exclude the header, and a CSV source hash is the parsed file hash.
- `Dataset.source_location(table, indexed_row)` and raw `.source_locations["message:<id>"|"image:<id>"]` expose immutable `{relative_path,row_number,csv_sha256}` locators so evidence need not reparse CSV or mistake a filtered-user index for a file record ordinal. **`csv_sha256` is not the decoded image/text leaf hash**; evidence computes the latter and source availability itself.
- `evaluation.samples.load_sample_fixtures(dataset_root)` is harness-only: 15 source columns become separate input and expected-output tuples. `evaluate_samples(dataset_root, decide, audit=None)` calls the supplied **trusted committed composition** with only the raw context; expected fields are joined afterward. There is no model, sample lookup, fallback classifier or dynamically selected CLI plugin inside this harness. It records missing/failed rows and sanitized exception classes rather than reporting a partial denominator as complete.
- `evaluation.metrics.compare_predictions(...)` keeps amount signed/absolute errors by home currency, normalized amount error, blank/date confusion and signed/absolute date errors, status/method confusion plus joint agreement, normalized schedule/action reference agreement and structural defects separate. No hidden weights are invented. Literal explanation match is not used. Real safety, schedule eligibility, action validity and grounding require a supplied audit adapter; missing evidence is explicitly `not_checked`, not a passing financial verdict. Error records start open; a detector does not invent a financial root cause, fix or regression ID.
- `evaluation.samples.persist_run(...)` creates a new run directory exclusively, retaining predictions (raw JSON and the exact eight-column CSV projection), metrics, per-request results, errors, failures and a summary. A hash manifest is written last; interrupted attempts without it remain incomplete. Existing run IDs and dataset destinations are refused. Expected fixture hashes are harness metadata, not inference evidence. Configuration supplied here must contain no secrets.
- `buy_wait.output.structural_issues(row, request)` checks the exact eight fields, enums, decimal/date/action grammar, bounds, partial two-payment arithmetic, basic installment sums, deadlines/horizon and status consistency. It is **not** supplied-option matching, preference/protection validation, balance replay or grounding. `write_output(..., dataset_root=...)` checks all structural rows before atomic replacement, enforces coverage/uniqueness/input order, preserves CSV quoting and refuses dataset paths. A future final runner must first obtain complete core/planning certification. A zero request does not acquire an invented positive payment minimum.

`python3 code/main.py --check-inputs` and `python3 code/evaluation/main.py` are read-only inspection commands. `python3 code/evaluation/main.py --predictions <public-development.csv> --run-id <new-id>` compares an existing development CSV; it performs no model or financial replay. Real-data checks remain input, selection, decode and support-validation tests, not a complete decision evaluation. Baseline/iteration inference and full trace diagnosis remain to be executed once the complete composition exists.

## Deterministic runner API (synthetic tests, not a final run)

`buy_wait.runner.plan_request(data, request, *, facts, evidence_sources=(), policy=None, max_candidates=None)` composes the real financial input/core, generated no-change phase, actual candidate evaluations, genuine P2 witnesses or the changed phase, complete search coverage, ranking and status. It returns a trace dictionary retaining the native financial records and planning-owned results; it does not introduce another financial classifier, Plan/result class or replay. A row is formatted only from resolved module conclusions and passes structural checks; formatting failures remain visible and produce no row.

`decide_cached(data, request, *, dataset_root, cache, usage, run_id, ...)` uses the real selection, local image loader, cache-only extractor and sole FactBatch adapter before that plan composition. It constructs **no live client/budget/key lookup** and never seeds a cache. Missing/invalid sources and unsupported host bindings remain explicit issues; bindings, when provided, must be trusted inputs to the existing adapter, not a new guesser. Callers bind `dataset_root` to the loaded participant tree and provide the canonical UsageSink. Source traces retain content/CSV/row hashes, cache keys, origins/issues, selected/excluded IDs and real origin UsageEvents. `trace_wire` uses core's canonical serialization. No raw model body or credentials are added to usage.

Nine synthetic tests cover full/partial/changed-full, exact unchanged safe amount, real replay and negative Stop deltas, P2/full changed coverage, truncation/missing-amount/fallback failures, label exclusion, missing images, host-binding identity, and actual mocked-origin cache→complete planning paths without a second dispatch. Cached120 debit yields580 no-change capacity on a600 request with no valid plan; a separately supplied550 request reuses the same fact, safely pays full and leaves330 above minimum300. A salary-backed partial pays580 then20. These are manufactured inputs, **not answer-derived caches or public-model accuracy**.

The whole local suite now passes **306 tests /1.780s** =297 prior dependency/package +9 runner. The source-tree entry point still supports inspection only; durable UsageSink/aggregation, CLI execution/audits, actual all25 baseline and final reproduction remain to be completed. Known dependency review limitations still block final certification.

## Provenance and immutable inputs

Only participant CSVs are used. The production loader does not read `sample_requests.csv`, `dataset/output.csv`, evaluation reports, prior predictions, organizer files or credentials. Raw file hashes refer to the exact parsed bytes, including quoting/newlines. Source record ordinals exclude the header; physical line numbers are a separate concept. Source selection and later financial admission must not turn future/unrelated messages, delivered status, pending approval, unrealized value or cropped text into settled cash.

Known crop: `image_04` / `event_1700` has INR 2,854.00 as a **visible subtotal**, not an established final paid amount. No prior research/diagram artifact was deleted or replaced to make this clarification. `image_05` / `event_1786` has two date-qualified bill amounts; its adapter must retain both, leaving applicability to core. Neither value is hardcoded into this implementation.

## Remaining owned work and release limits

The controlled initial core dependency is source `f60e498b42f9f76d8fe3f6ffeb5a4d8d12a0a7b4`, imported with provenance as local `19b87510f55b2cd73f0c27e475545eebd3f63596`. Integration's typed handoff above uses its **actual** constructors; imported core files remain unmodified by integration. Real CSV-to-core analytic tests now cover pending holds/atomic settlement, bill-before-salary capacity and exact partial replay, native-to-home FX, missing-debit proof failure, source-backed fact admission, supported monthly recurrence, negative changed-expense deltas and explicit-only proof guards. All25 public fixtures and 71 offers also pass typed projection **without forecasts or predictions**. Planning was initially a separate helper-only dependency; inbox015 now imports its complete routed module below. Static success alone is never replay safety.

Core's actual `Plan` fields are `candidate_id` and `payment_option_id`, with `core_hash`/`envelope_hash`; request identity is derived through the bound contexts. Options must adapt its provisional `plan_id`/`supplied_option_id` view rather than introduce aliases in core or duplicate Plan here. The initial recurrence slice was not a complete forecast. Separately authorized follow-on source `714ed4241e735d2d38cf7645b95bb824700c0bb2` is now imported as `d3dc4c8d4195dea0333b5a5b1a7cb52c6425b6fa`, without changing imported files. It implements the supported recurrence/coverage policy; explicit-only mode still returns unresolved `EXPLICIT_ONLY_NOT_FULL_PROOF`, and material unknowns still cannot become zero. Expense `occurrence_deltas` mean new minus old debit magnitude in home minor units: reductions are negative. This is a development dependency, not a green/landed core release; its separate no-mistakes validation may produce a later authoritative head. No moving sibling filesystem or future commit is implicitly authorized.

**Corrected development boundary (inbox015):** the complete authorized core chain `788fc4c482b6949393a34ff098cfc94d7fc6b977` → `daafdfdfdc18b271c1870249c4ddd1d6f21d06ea` → `38ce2590f997113d6cb5904ed77ff8d6e427163d` is now imported as `ff6337d5cd661d86838f2769256e279362ea281a` → `ccc8402e0addbc4024326e68480662cd9cab1068` → `8ffe945f1daf6edd10360e146c34fcac13381616`. R1/R2 matching and both unknown-date correction rounds/positive controls pass locally; the intermediate788fc4c4 alone was not sufficient. F2 retains usable conservative salary bounds without double-counting or confidence upgrades. Imported core paths match38ce2590 exactly; integration made no core edits.

Core PR https://github.com/arbadev/mondanator/pull/2 has completed local pipeline review/test/docs/lint but, per Firstmate's handoff, **zero registered GitHub checks**, Actions enabled and zero workflows. This is a separate owner-investigated CI gap, **not CI-green, landing, skipped-CI permission or final delivery certification**. Later pipeline commits still need exact routing.

### Controlled evidence import (inbox014)

Preserved the owned119-test packaging/setup checkpoint at `721c105116fe99a3680ad9f5294445af4e72a202`, verified the original isolated branch, zero current-branch validation runs, exact source parents/paths and no previous imports, then ordinary clean `cherry-pick -x` imported only:

| Authorized evidence source | Local result |
|---|---|
| `a6767841f252fbc9738190f5bc11928acdf2ce4d` | `b3c528a602a0aea0f17039fc6ce3a3aaa75b2975` |
| `d78da7559538de140afcc7970e6cc17b5c38dcdd` | `750631cab260ae6c46e44da2de80109f956f1f80` |
| `06a636287b47318898f206f98bc6bafc542666f7` | `4a7107e80046014eaa407ba1cc9ea884c28a958b` |
| `49c26eb72692ff8ddf956f38129b72c5d7bc3e5a` | `86ce4451bbae9d4def6fb917db59b0bf17aa9c05` |
| `b7be3b582db2db5de7e78aa153649c34c84952d1` | `3245d43da082b8bc6505c69c24d71b10a146981f` |

No duplicate core/integration imports, conflicts, force/stash/discard or primary/sibling edits. At inbox014, core paths matched714ed424 exactly; inbox015 advances them through the complete reviewed chain above. Evidence paths are evidence-owned and remain unmodified here. Both owners' active pipelines may produce later corrections, requiring separate exact handoffs.

Use `EvidenceIndex.from_context(data.context_for(row)).retrieve(...)`, the core event map via `event_descriptors`, local `resolve_image`, and `Extractor.extract(..., mode="cache-only")`. Its single `adapt_extraction(...) -> AdaptedEvidence` supplies native `FactBatch` and SourceRefs to `data.financial_input_for(..., evidence_sources=...)`. Missing material cache evidence yields explicit issues/null proof, never a fabricated cache or zero. Source rows, decoded leaves and file hashes stay distinct. Tests prove120 pending debit→580 safe capacity above300 minimum and no second mocked HTTP request on cache replay; this is not a completed purchase recommendation.

Evidence owns `UsageEvent` and `UsageSink`; `event.to_dict()` retains canonical physical attempt IDs, retry relationships, outcome/model/provider, nullable token counts/cost strings, and cache-origin attempt references. Integration must durably persist/aggregate those events and returned `ExtractionResult.origin_usage`, not invent another billable-call counter. New cache hits have zero new cost but consumed origin costs are separate; unknown usage remains unknown. No live client is needed for cache-only composition.

### Open evidence review limitations (inbox016)

The imported `b7be3b582db2db5de7e78aa153649c34c84952d1` boundary still has two financial-adapter findings in active review `01M2BXNHDVGYJDC27JR94ZMHK4`:

- `approval-obligation-copies-csv-status`: metadata-only approval/obligation observations copy the event's CSV cash status into a sourced StateClaim. The review's pending-bill example has an explicit settled fact plus an obligation-closed fact that reasserts pending; this can prevent the genuine settlement from winning, or mask settlement of a credit.
- `unspecified-window-becomes-permanent-series`: a host-bound SeriesTarget with unspecified duration becomes an all-future-cycle amendment; a10% salary increase can therefore become permanent without supporting scope evidence.

These are reported review cases, not independently reproduced/fixed by integration. The owner/Firstmate must route corrected committed interfaces; **do not repair imported code, answer the evidence gate or import an unlisted pipeline head**. All297 passing tests remain real but do not establish absence of these defects. No final output, package or complete correctness claim is allowed against this known boundary.

The same review lists stale setup/docs on the evidence branch and the POSIX/fcntl limit. Integration's agreed Python3.13 exact-pin setup and docs have already been implemented/locally installed and tested here; that does not claim the evidence pipeline's findings are closed. Native Windows remains unsupported. Continue deterministic runner/accounting tests without a new model-driven loop or paid calls.

### Open planning review limitations (inbox017)

Review `01M2BYWJQ9QNW24YXGHCYR0TBC` at the owner's corrected-core branch head `3b81413067d0fdf8839920ea173bc864de87e5bf` identifies four still-open findings. It does **not** authorize importing that head or choosing a new policy:

- `superset-change-serialization`: a tied two-action ReduceTo+Stop plan can serialize ahead of a sufficient Stop-only plan, recommending an unnecessary reduction. Strict-subset dominance or a new action-count priority is not authorized here.
- `stale-docs-provenance`: the options branch retains older integration docs. This checkout's controlled import provenance is already updated; the other branch's finding remains pipeline-owned.
- `blank-cap-unverifiable`: a blank installment cap is treated as unverifiable instead of a definite refusal, potentially blocking a safe full-payment plan.
- `full-today-suppressed-by-excluded-row`: an excluded full-payment offer can suppress a rule-based full-today candidate and delay changed one-shot generation.

All four were initially authority-gated, including the info-severity findings; absence from current data is not acceptance. Inbox019 subsequently settles the stale-docs fix, definite refusal for a validated blank cap, and bounded full-today correction that preserves credible seller dates/fees, relevant uncertainty and actual equivalent-offer deduplication. Their implementation is not claimed complete. **The same-schedule/option, independently validated unnecessary-action subset rule remains a captain choice on existing bw-options-r5**, not a general fewer-actions priority or an adopted policy. The options review stays parked for one consolidated response. Integration must not change imported planning, repair the selection in its renderer or invent ranking semantics. Preserve these issues beside the extraction limits and await exact corrected options/extraction handoffs before final certification. The297-test dependency checkpoint preceded the nine synthetic runner tests described below; the combined306-test result does not resolve these review findings.

### Controlled planning import (inbox015)

At clean corrected-core checkpoint8ffe945 (202 tests passing), imported only the authorized planning-only commits with `-x`:

| Planning source | Local result |
|---|---|
| `3e52b574d691969eecb5bacb7bc18efcce8fb556` | `ca691d75ba26b353cfa717ad10ac72a977ee783e` |
| `ea9a7450b165025077a65ec7c463ae9d92d75454` | `23a373994648af02c09a53fd1a1f222fe3643e82` |
| `bd637b1adc2ac9b7b0ec5727cfe511bdcc76ad96` | `ce0077f6e42c9c0510e05d3e2395ec33d2374b18` |
| `ed0d099b6a585596c22f32938297a6837847d753` | `28cf874f31bb248b0cfbeedd77e45a86944ac8aa` |

Paths match the pinned planning source; no duplicate core/integration imports or consumer hand-edits. `generate_candidates`, `evaluate_candidate`, `build_search_coverage`, `rank_validated` and `recommendation_status` return planning-owned dictionaries containing canonical Plans/Money/replay and exact identities. Preserve full families/exclusions/validations/coverage and unchanged capacity. Changed-phase pruning needs an actually validated eligible on-time no-change witness. Incomplete/unverifiable search and `FALLBACK_STATUS_UNSPECIFIED` cannot become certified rows. Tests include95 planning methods (static helpers plus real replay/CSV integration); the test-only `run_phases` demonstration is not a production runner.

All 297 tests pass in1.570s on the local3.13.15 environment after both full chains. The13 packaging tests pass; their previously reproduced identity-coercion/private-field failures were fixed in721c105 and are retained as regressions, not waived. No complete production runner test or final artifact is claimed yet.

Still required: full deterministic/cache-aware orchestration and financial/grounding audit; all-25 inferred baseline and retained root-cause/regression iterations; durable canonical usage aggregation and actual final-run accounting; final artifact preparation and clean application reproduction. Direct dependency pins and3.10/3.13 CI now follow the evidence handoff above, not the retired dependency-free3.9 setup.

Final artifact policy remains `code.zip`, root `output.csv`, and separate `chat_transcript`; one actual run-bound `evaluation/usage_report.md` must sit at archive root. The empty nested starter report is removed rather than presented as usage evidence. Account newly incurred physical attempts separately from consumed cache origins, including retries/failures/unknown usage. Packaging must be allowlist-only, exclude `.env`, credentials, dataset copies, organizer files, Git/venv/bytecode, irrelevant caches and operational logs, and prove output/accounting hash identity. The retained primary transcript has a historical-integrity limitation; no rewrite or restoration is authorized.

### Independent packaging capability (synthetic tests only)

`evaluation.package.build_code_zip(root, manifest_relative, destination="code.zip")` and its CLI (`--root`, `--manifest`, `--output`) require a frozen `buy-wait-final-artifacts/v1` manifest. It must identify an actual final run, ordered/structurally valid output hash/count, participant input pins, complete supported per-request proof states, zero critical issues, complete accounting and explicit archive-member hashes. A usable `conservative_bound` is not automatically rejected. These are trusted runner assertions checked for consistency, **not independent financial certification or captain permission**; no actual final manifest is produced at this checkpoint.

Exactly one archive-root `evaluation/usage_report.md` must contain a `<!-- buy-wait-usage/v1 <JSON> -->` line whose JSON has exactly `run_id` (nonempty string), `request_count` (positive integer), `output_sha256` (64 lowercase hex characters), and `accounting_complete` (boolean). These must match the manifest. Numeric/boolean coercion and extra identity fields are refused. Actual human-readable usage details and the canonical UsageEvent aggregator are still pending; a matching marker alone does not establish billing truth.

The builder validates every member name before opening members, rejects hidden/credential/organizer/dataset/cache paths, symlinks and traversal/Windows stream syntax, and applies bounded recognizable-secret/JSON-key guards (not a universal secret detector). It verifies member hashes through a deterministic ZIP round trip and publishes exclusively without replacing an earlier ZIP. `output.csv` and `chat_transcript` remain separate artifacts. Unit tests create synthetic bundles only beneath `.test-tmp/`; a toy program's extracted execution is not clean reproduction of the financial application. No real `code.zip`, final usage report or transcript export was generated.

No paid calls, key reads, contest uploads, final evaluation predictions or real final packaging are authorized by this checkpoint. The all-25 harness has not yet run an actual complete decision composition, and malformed audit payloads cannot claim checked safety or convert unknown violations to zero. Preserve the 16:00–18:00 IST submission buffer on September 13, 2026. Each merge still needs individual captain approval; this work does not claim a landed or fully validated agent.
