# Required Python >=3.10; tested baseline Python 3.13.
# Exact direct pins for offline evidence client/schema/image checks, not a transitive lock.
# Secrets are environment-only; no automatic .env loading or speculative model SDKs.
pydantic==2.13.5
httpx==0.28.1
Pillow==12.3.0
