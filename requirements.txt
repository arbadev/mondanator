# Integration/explicit-core checkpoint: Python >=3.9, standard library only.
# No third-party packages are required by these offline modules/tests.
# The evidence owner's committed client dependencies must be pinned at handoff;
# do not install speculative model SDKs or automatically load .env.
