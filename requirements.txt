# Python >=3.10 is required by the evidence module; the tested baseline is Python 3.13.
# Exact pins for the offline evidence client, schema and image checks.
# Do not install speculative model SDKs or automatically load .env.
pydantic==2.13.5
httpx==0.28.1
Pillow==12.3.0
